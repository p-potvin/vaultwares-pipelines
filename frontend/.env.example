# Backend API base URL (vaultwares-pipelines FastAPI server)
# Leave empty to use the Vite dev proxy (proxies /api/* to localhost:8000 by default)
# In production set this to the deployed API URL, e.g.:
#   VITE_API_URL=https://api.example.com
VITE_API_URL=
