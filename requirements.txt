# ============================================================
# vaultwares-api - core runtime dependencies
# ============================================================

# ---- image processing ----
Pillow>=12.3.0
numpy>=1.24.0

# ---- 3D processing ----
open3d>=0.18.0
trimesh>=4.0.0
gradio>=6.22.0
GPUtil
pynvml
matplotlib
diffusers

# ---- video processing (optional but strongly recommended) ----
opencv-python>=4.8.0


# ---- API & docs ----
fastapi
uvicorn[standard]
psutil>=5.9.0          # memory_info() in DeviceManager
tortoise-orm<1.0          # 1.x made _inited context-scoped which breaks FastAPI startup-event init
asyncpg                    # Required by tortoise-orm for Postgres async driver
websockets                 # Used by the job worker to stream ComfyUI progress events
python-jose[cryptography]
bcrypt==4.0.1
passlib[bcrypt]

# ---- Prom-King router (vaultwares-api /api/promking/*) ----
# Talks to the `promking` Postgres DB via asyncpg (already pinned above for
# tortoise-orm). APScheduler runs the nightly fetch cron in-process —
# replaces the ADR-001 node-cron plan (router lives on the Python side).
# See ADR-001 'Implementation deviations' for the rationale.
apscheduler>=3.10

ipython>=9.10.1
redis>=7.0.0
pyinstaller>=6.0.0

pydantic
# For persistent storage integrations (to be implemented):
# google-api-python-client  # Google Drive
# dropbox  # Dropbox
# pyicloud  # iCloud

rembg>=2.0.50
webauthn
